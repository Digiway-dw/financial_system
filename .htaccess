<IfModule mod_rewrite.c>
    RewriteEngine On

    # استثناء مجلد التحقق بتاع الشهادة
    RewriteCond %{REQUEST_URI} ^/.well-known/ [NC]
    RewriteRule .* - [L]

    # تحويل باقي الطلبات إلى /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
